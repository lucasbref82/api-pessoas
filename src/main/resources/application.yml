spring:
  datasource:
    url: jdbc:h2:mem:testeattornatus
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true